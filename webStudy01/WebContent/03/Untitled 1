1.request분석하기
1) Request.Line
	서버상에 존재하는 자원의 존재값 Request Line : URL,Method-get(조회, R),post(C) put(U), delete(D) method. 
   4가지(get, post, put, delete) //url을 대상으로
       사전요청 options를 보내 지원여부 응답을 받는다.
   	request Line -> 현재 명령에 대한 정보
2) requset Header
	- 
3) Request.body
	- 메시지를 집어 넣기 위해 필요한 영역(보낼 메시지가 없을 경우 필요x)
	- 서버에 메시지를 보낼 때 필요(only POST일 경우)